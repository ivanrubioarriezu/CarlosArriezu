# Optional deployment configuration.
# Standard GitHub Pages deployments derive BASE_PATH from the repository name.
# For a custom domain, use the domain and root path shown below in the workflow.
# SITE_URL=https://artist.example
# BASE_PATH=/
